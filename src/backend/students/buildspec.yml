version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  pre_build:
    commands:
      - docker --version
      - echo Pre-Build
      - sam local start-api
  build:
    commands:
      - echo Build
      - sam validate
      - sam build

artifacts:
  files:
    template.yaml